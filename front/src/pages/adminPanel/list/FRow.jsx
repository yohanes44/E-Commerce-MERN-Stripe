

export default function FindRow (){
    // {row, fieldName, fieldValue}
    // let foundRw = rows.find( (rw) => rw.id == row.id)
    // console.log({foundRw});
    return <div className="cellAction" style={{ display: "flex", flexDirection: "column" }}>
            <div>jooo</div>
             {/* {true ? <input type='text' value={rw.value} onChange={(e) => {}} /> : null} */}
        </div>

  }